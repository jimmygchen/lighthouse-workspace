Review this PR: $ARGUMENTS

Use a claude team. Split the diff into domains by crate/layer, review in parallel (both security and quality per agent), then cross-review to reduce false positives. Compare new code against analogous existing patterns. Read CLAUDE.md and code review guidelines first.
